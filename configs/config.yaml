experiment_name: "baseline_experiment"
seed: 42
device: "cuda"

# avoid hardcoding paths in the code
data:
  path: "data/processed"
  batch_size: 32
  num_workers: 4

model:
  name: "ResNet18"
  input_channels: 3
  num_classes: 10

training:
  epochs: 50
  learning_rate: 0.001
  save_dir: "checkpoints/"